# CLAUDE.md

Start by reading `AGENTS.md`.

`AGENTS.md` is the repo-specific source of truth for:

- implementation scope
- approved tooling
- dependency policy
- testing expectations
- artifact handling

Before changing a pipeline stage, also read the matching document under `docs/specs/`.

If there is any conflict between this file and `AGENTS.md`, follow `AGENTS.md`.
